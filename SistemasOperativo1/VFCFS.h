#pragma once
#include <windows.h>

// funciones
void CreateVFCFS(HWND hDlg);
void MostrarFCFS();

// variables globales (IMPORTANTE)
extern HWND hBoxArchivo;
extern HWND hBoxTabla;
extern HWND hBoxGantt;
extern HWND hBoxResultados;
