#include <windows.h>
#include "resource.h"

IDD_MAIN_DIALOG DIALOGEX  0, 0, 700, 350
STYLE DS_SETFONT | WS_OVERLAPPEDWINDOW | WS_VISIBLE| WS_CLIPCHILDREN
CAPTION "Data Structure Visualizations"
FONT 10, "Verdana"
BEGIN
END
